package ru.itis.helper;

public class CheckerTheDate {

//    public void sendingMessage(Long id) {
//
//        EmailServiceImpl emailService = new EmailServiceImpl("memetovaevelina360@gmail.ru", " ");
//
//        SubscriptionServiceImpl subscriptionService = new SubscriptionServiceImpl();
//        UserServiceImpl userService = new UserServiceImpl();
//        List<Subscription> list = subscriptionService.getSubscriptionsByUserId(id);
//        for (Subscription subscription : list) {
//            if (subscription.getEndingDate().equals(new Date()))
//                emailService.sendMessage(subscription, userService.findUserById(id).getEmail()); }
//    }
}
